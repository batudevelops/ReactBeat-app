/** Bundled image assets (see /assets at repo root). */
export const images = {
  logo: require('../../assets/logo.png'),
  splash: require('../../assets/splash.png'),
  icon: require('../../assets/icon.png'),
} as const;
